import { NotFoundPage } from '@/components/platform/page-templates';

export default function PartnerManagerNotFoundPage() {
  return <NotFoundPage title="Partner-manager route not found" body="Return to the assigned-firms workspace or dashboard to continue onboarding and relationship tasks." href="/partner-manager/dashboard" label="Back to Dashboard" />;
}
