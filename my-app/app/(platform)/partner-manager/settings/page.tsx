import { SettingsPageTemplate } from '@/components/platform/page-templates';

export default function PartnerManagerSettingsPage() {
  return (
    <SettingsPageTemplate
      accent="#1a6b4a"
      title="Partner Manager Settings"
      description="Profile, contact verification, and reminder settings for partner-facing platform work."
    />
  );
}
